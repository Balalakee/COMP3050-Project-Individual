global:
  scrape_interval: 5s    # How often Prometheus scrapes each target

scrape_configs:
  - job_name: 'java-app'            # A human-readable name for this group of targets
    static_configs:
      - targets: ['server:8000']        # The hostname and port to scrape
